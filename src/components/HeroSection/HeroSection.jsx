import { Container } from "react-bootstrap";
import Cards from "./Cards/Cards";

const HeroSection = () => {
   return (
      <Container>
         <h1>Hero Section</h1>
         <Cards />
      </Container>
   );
};

export default HeroSection;
